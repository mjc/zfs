# Keep the active shell in the QEMU-capable dev environment by default.
use flake .#qemu

# Nix hosts should avoid Ubuntu runner cleanup/package churn by default when
# running the copied Nix bootstrap script locally.
export NIX_QEMU_SKIP_HOST_CLEANUP="${NIX_QEMU_SKIP_HOST_CLEANUP:-1}"
